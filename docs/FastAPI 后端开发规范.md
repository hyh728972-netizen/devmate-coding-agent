# FastAPI 后端开发规范

- 所有接口应遵循 RESTful 风格设计原则。
- 涉及数据库操作或网络请求的接口，应使用 async 异步方式实现，以提升并发性能。
- 请求参数与响应结构必须使用 Pydantic 模型进行定义和校验，保证数据格式的规范性与安全性。
- 所有路由统一放置在 `/api/v1` 路径前缀下，便于接口版本管理与系统扩展。
- 日志输出必须使用 Python 的 logging 模块，禁止使用 print 进行调试或运行日志输出。